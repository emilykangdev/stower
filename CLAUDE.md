# Stower — Agent Conventions

The canonical rule set lives in `AGENTS.md` so every agent — Claude Code, Codex,
and any other — reads the same rules. The import below loads it into Claude Code's
context at launch. Do not add rules here; edit `AGENTS.md`. Put Claude-only
instructions (if any ever arise) under the "Claude Code" heading below.

@AGENTS.md

## Claude Code

(No Claude-specific overrides yet. Everything lives in `AGENTS.md`.)
