# StowerPhotos

Photos data-source adapter: PhotoKit enumeration, FastVLM caption pipeline, and
Vision OCR. Produces `IndexedItem` values for `StowerCore`. Must not import
`StowerMessages`.

Subsystem rationale and planned API surface: [`Docs/StowerPhotos.md`](../../Docs/StowerPhotos.md).
